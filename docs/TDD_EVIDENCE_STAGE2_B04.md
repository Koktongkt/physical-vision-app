# Stage 2 B04 TDD Evidence

All production behavior was developed as vertical RED -> GREEN slices. Because the installed `uv` was 0.12.0 while the repository pins 0.11.31, executable evidence uses `uvx --from uv==0.11.31 uv run ...`. The first direct `uv run` attempt correctly stopped on the version mismatch; no gate was reported as passing from that command.

## Observed slices

| Slice                                               | RED command and observed failure                                                                                                                                                            | GREEN command and observed result                                                                                                                                                                                                               |
| --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Package boundary / positive unsupported             | `... pytest tests/python/test_policy.py::test_positive_unsupported_evidence_is_terminal_before_other_failures -q` -> collection failed with `ModuleNotFoundError: physical_vision_policy`   | Same targeted command -> `1 passed`; full suite -> `156 passed`                                                                                                                                                                                 |
| Distinct no-label outcome                           | `...::test_no_label_is_distinct_from_ocr_failure -q` -> expected `no_label`, got `manual_required`                                                                                          | Target -> `1 passed`; full suite -> `157 passed`                                                                                                                                                                                                |
| Stale evidence rejection                            | `...::test_stale_snapshot_is_rejected_instead_of_normalized -q` -> import failed because `PolicyInputError` did not exist                                                                   | Target -> `1 passed`; full suite -> `158 passed`                                                                                                                                                                                                |
| Policy compatibility                                | `...::test_snapshot_version_compatibility_must_match_frozen_config -q` -> expected `PolicyInputError`, none raised                                                                          | Target -> `1 passed`; full suite -> `159 passed`                                                                                                                                                                                                |
| Threshold compatibility                             | `...::test_snapshot_threshold_compatibility_must_match_frozen_config -q` -> expected `PolicyInputError`, none raised                                                                        | Target -> `1 passed`; full suite -> `160 passed`                                                                                                                                                                                                |
| Reliable one-action guidance                        | `...::test_reliable_correction_produces_exactly_one_camera_action -q` -> expected `guidance`, got `manual_required`                                                                         | Target -> `1 passed`; full suite -> `161 passed`                                                                                                                                                                                                |
| Strict automatic gate                               | `...::test_all_gates_and_probability_strictly_above_pet_are_automatic_eligible -q` -> expected `automatic_complete`, got `manual_required`                                                  | Target -> `1 passed`; full suite -> `162 passed`                                                                                                                                                                                                |
| Exact PET candidate boundary                        | `...::test_probability_exactly_at_pet_is_candidate_ready_not_automatic -q` -> expected `ready_for_verification`, got `manual_required`                                                      | Target -> `1 passed`; full suite -> `163 passed`                                                                                                                                                                                                |
| Deep immutability                                   | `...::test_policy_preserves_inputs_and_returns_deeply_immutable_decision -q` -> mutation did not raise                                                                                      | Target -> `1 passed`; full suite -> `164 passed`                                                                                                                                                                                                |
| Deterministic content identity                      | `...::test_decision_identity_and_canonical_output_are_deterministic -q` -> distinct snapshots received the same placeholder identity                                                        | Target -> `1 passed`; full suite -> `165 passed`                                                                                                                                                                                                |
| Frozen priority/cost/tie config                     | `...::test_default_policy_configuration_freezes_priority_cost_and_tie_break_rules -q` -> config lacked `outcome_priorities`                                                                 | Target -> `1 passed`; full suite -> `166 passed`                                                                                                                                                                                                |
| Cost selection                                      | `...::test_fixed_cost_selects_candidate_when_automatic_and_candidate_are_both_safe -q` -> expected candidate, got automatic                                                                 | Target -> `1 passed`; full suite -> `167 passed`                                                                                                                                                                                                |
| Stable tie break                                    | `...::test_stable_tie_break_selects_automatic_when_candidate_costs_are_equal -q` -> insertion order selected candidate instead of automatic                                                 | Target -> `1 passed`; full suite -> `168 passed`                                                                                                                                                                                                |
| Frozen vectors and dual consumers                   | `...::test_frozen_policy_vectors_match_and_validate_in_python_and_node -q` -> vector manifest absent                                                                                        | After adding vectors, the first run reached Node and failed closed because dependencies were not installed; `npm ci` reported 0 vulnerabilities, then target -> `1 passed`; full suite -> `169 passed`                                          |
| Known OOD blocking gate                             | `...::test_known_out_of_distribution_evidence_sets_blocking_gate -q` -> expected `no_unknown_blocking=false`, got `true`                                                                    | Minimal correction requires `ood_state=in_distribution`; target -> `1 passed`; full suite -> `175 passed`                                                                                                                                       |
| Exact frozen config collections                     | `...::test_policy_config_collections_fail_closed_when_not_exact -q` -> `8 failed, 1 passed`; missing/duplicate/unknown configured keys could be accepted and a missing cost could fail open | Added exact key-set/uniqueness validation before evaluation; all nine cases -> `9 passed`; policy file -> `29 passed`; full suite -> `184 passed`                                                                                               |
| Version-bound frozen semantics                      | `...::test_policy_v31_rejects_unversioned_semantic_drift -q` -> exact-key value mutations retained `policy-v31` while changing hard-veto/cost behavior                                      | Bound policy-v31 to its exact threshold, priority, cost, and tie vectors; five semantic-drift cases plus two matching-label unregistered vectors fail closed                                                                                    |
| Non-bypassable recursive immutability               | Reviewer probe `dict.__setitem__(decision, ...)` mutated the earlier `dict` subclass and nested gates                                                                                       | Replaced the mutable base with `FrozenMapping`; direct and base-class mutation probes fail, explicit detached document/canonical JSON conversion passes; policy file -> `34 passed`; full suite -> `189 passed`                                 |
| Exact runtime types and registry-owned execution    | Hostile `int`/`float`/`str` subclasses and `bool == 0` could compare equal to registered values while overriding policy operations; targeted RED -> `5 failed, 4 passed`                    | Require exact built-in container/scalar types and execute only registry-owned PET/order vectors; hostile-type target -> `9 passed`; policy file -> `43 passed`; full suite -> `198 passed`                                                      |
| Detached evidence, private registry, sealed backing | Hostile nested mapping, exported-config mutation, and backing-slot reassignment regressions -> `3 failed`                                                                                   | Recursively detach evidence before validation/execution, resolve against private constants independent of the exported config, and reject mapping slot assignment; target -> `3 passed`; policy file -> `45 passed`; full suite -> `200 passed` |

The later table-driven ordering, unknown-veto, unreliable-correction, freshness-cutoff, malformed-config, version-binding, hostile-mutation, scalar-subclass, caller-mapping, registry-mutation, and backing-slot assertions exercise the deterministic resolution and immutable boundary. Their focused file run reported `45 passed`; the complete suite then reported `200 passed`.

## Final acceptance commands

Observed on the complete working tree:

- `uvx --from uv==0.11.31 uv run ruff format --check .` -> `6 files already formatted`.
- `uvx --from uv==0.11.31 uv run ruff check .` -> `All checks passed!`.
- `uvx --from uv==0.11.31 uv run pytest -q` -> `200 passed`.
- `npm run contracts:check` -> generated contract types in sync.
- `npm run typecheck` -> passed.
- `npm run test:ts` -> `141` passed, `0` failed.
- `npm run format:check` -> all matched files use Prettier style.
- `npm audit --audit-level=moderate` -> `0 vulnerabilities`.
- `uvx --from uv==0.11.31 uv run python scripts/check_sensitive_files.py` -> passed for `202` tracked files.
- `git diff --check` -> passed with no output.

This file must not be read as evidence for physical guidance, calibration quality, production readiness, or any deferred Stage 3+ integration.
